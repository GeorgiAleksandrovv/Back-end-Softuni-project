

        <section class="search">
            <h1>Search</h1>

            <form class="search-form"  method="GET">
                <input type="text" class="search-name" name="name" placeholder="Search Name...">
                <input type="text" class="search-type" name="searchType" placeholder="Search Type...">
                <button type="submit" class="btn-search">Search</button>
            </form>

            <!--If there are matches after search-->
            {{#if offer}}
            <div class="search-result">
                <div class="search-list">   
                    {{#each offer}}
                    <a href="/catalogs/{{_id}}/details" class="added-electronics-in-market">
                        <img src="{{image}}" class="picture-added-electronics">
                        <h3>{{name}}</h3>
                        <span>Price: {{price}}</span>
                    </a>
                    {{/each}}
                </div>
                  {{else}}
                  <div class="no-match">
                <p>No match was found!</p>
            </div>
            {{/if}}
            </div>
            <!--If there are no matches-->
            <!-- <div class="no-match">
                <p>No match was found!</p>
            </div> -->
        </section>

  